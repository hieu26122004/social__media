const ROOT_URLS = {
  HOME: "/",
};

export const PATHS = {
  HOME: `${ROOT_URLS.HOME}`,
  LOGIN: `${ROOT_URLS.HOME}login`,
  REGISTER: `${ROOT_URLS.HOME}register`,
  POST_DETAIL: `${ROOT_URLS.HOME}post/:postId`,
  USERS: `${ROOT_URLS.HOME}users`,
  NEWS: `${ROOT_URLS.HOME}news`,
  SETTING: `${ROOT_URLS.HOME}settings`,
  DELETE_POST: `${ROOT_URLS.HOME}delete/:postId`,
};
